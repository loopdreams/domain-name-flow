(ns domain-name-flow.domain-name-flow
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X domain-name-flow.domain-name-flow/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m domain-name-flow.domain-name-flow"
  [& args]
  (println "-main with" args))
